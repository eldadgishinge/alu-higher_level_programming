const Rectangle = require("./0-rectangle");

//#!/usr/bin/node
module.exports = class Rectangle {
    constructor (w, h) {
      this.width = w;
      this.height = h;
    if (w <= 0 || h <= 0) {
        let obj = {}
    }
    }
  };


